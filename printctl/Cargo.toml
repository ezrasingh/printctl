[package]
name = "printctl"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.32", features = ["derive", "env", "string"] }
config = "0.15.11"
serde = { version = "1.0.219", features = ["derive"] }
thiserror = "2.0.12"
printctl-runner = { path = "../printctl-runner", version = "*" }
